# Production — both web and mobile must use the SAME backend URL.
VITE_API_URL=https://fastapi-n7sg.onrender.com
VITE_WS_URL=wss://fastapi-n7sg.onrender.com

# Do NOT set VITE_NO_AUTH=true in production — it keeps the web app on fake local data
# and prevents sync with mobile.
x``